# Shooooort

> Link shortener

To install, run:

```
npm install

```

For dev webpack build, run:

```
npm run dev

```

For prod webpack build, run:

```
npm run prod

```

To start, run:

```
npm start

```